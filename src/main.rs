// Tiny stub - the real entry point is filled in by the implementation step.
// Kept compilable so that `cargo build` works in the dev-env setup commit.
fn main() {
    eprintln!("nix-pretty: not yet implemented");
    std::process::exit(2);
}
